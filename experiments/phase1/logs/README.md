# Phase 1 Logs

Run:

```bash
npm run experiment:phase1
```

Each JSON log must include:

- `raw_case`
- `system_output`
- `human_feedback`
- `failure_points`

Logs are synthetic only until the governance process explicitly approves real-data handling.
